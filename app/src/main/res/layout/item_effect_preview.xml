<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <FrameLayout
        android:id="@+id/container_preview"
        android:layout_width="@dimen/effect_preview_side"
        android:layout_height="@dimen/effect_preview_side"
        android:clickable="true"
        android:foreground="@drawable/fg_effect_preview">

        <ImageView
            android:id="@+id/image_preview"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginBottom="1dp"
            android:scaleType="centerCrop"
            app:srcCompat="@mipmap/ic_launcher"/>

        <com.redrocket.photoeditor.presentation.effects.view.previews.HighlightView
            android:id="@+id/highlight"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:visibility="invisible"
            app:startColor="@android:color/transparent"
            app:endColor="@android:color/transparent"
            app:frameColor="@color/colorAccent"
            app:frameWidth="6dp"
            />
    </FrameLayout>

    <TextView
        android:id="@+id/text_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:text="green"
        android:textColor="@color/colorTextIcons"
        android:textSize="15sp"
        />
</LinearLayout>